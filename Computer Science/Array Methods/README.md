# Array Methods

An area for rebuilding common array methods in order to best understand them. 

## Definitions:
- `every()`
- `filter()`
- `flat()`
- `forEach()`
- `Array.from`()
- `map()`
- `reduce()`
- `some()`
- `sort()`